apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radar-backend-logs
  namespace: radar-backend
  labels:
    app: radar-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard # Adjust based on your cluster's storage classes
